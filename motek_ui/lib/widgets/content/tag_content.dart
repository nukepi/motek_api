import 'package:flutter/material.dart';
import 'package:motek_ui/screens/tags_screen.dart';

class TagContent extends StatelessWidget {
  const TagContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TagsScreen();
  }
}
